<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Dreamy Slideshow with Magic Buttons</title>
  <style>
    :root{
      --accent1:#ff9ecb;
      --accent2:#c8a2ff;
    }
    * { box-sizing: border-box; margin:0; padding:0; }
    body {
      font-family:"Comic Neue", cursive, system-ui;
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:100vh;
      overflow:hidden;
      background: linear-gradient(135deg,#ffd1f3,#f6d0ff,#e5c9ff,#ffd6eb);
      background-size:400% 400%;
      animation: bgShift 20s ease infinite;
    }
    @keyframes bgShift {
      0% { background-position:0% 50%; }
      50% { background-position:100% 50%; }
      100% { background-position:0% 50%; }
    }
    #floating { position: fixed; top:0; left:0; width:100%; height:100%; pointer-events: none; z-index:0; }
    #floating span { position:absolute; font-size:18px; animation: floatUp linear infinite; opacity:0.6; top:100%; }
    @keyframes floatUp { 0% { transform: translateY(0) rotate(0deg); opacity:0.9; } 100% { transform: translateY(-120vh) rotate(360deg); opacity:0; } }
    .wrap { position: relative; z-index: 1; max-width: 1100px; width: 100%; display: grid; grid-template-columns: 1fr 420px; gap: 32px; align-items: center; }
    @media(max-width:900px) { .wrap { grid-template-columns:1fr; padding:0; } .right { order:-1; } }
    .headline { font-size: clamp(28px,5vw,44px); line-height:1.2; margin-bottom:18px; color:#6b217e; text-shadow: 0 2px 8px rgba(255,255,255,0.7); }
    .lead { color:#843b97; margin-bottom:22px; }
    .device { width:100%; max-width:420px; margin:auto; background: rgba(255,255,255,0.5); backdrop-filter: blur(12px); border-radius:28px; padding:18px; box-shadow: 0 20px 50px rgba(193,107,255,0.5); border:2px solid rgba(255,255,255,0.7); }
    .screen { background:#000; border-radius:18px; overflow:hidden; position:relative; height:640px; display:flex; align-items:center; justify-content:center; }
    .screen img { width:100%; height:100%; object-fit:cover; position:absolute; opacity:0; transition:opacity 1.2s ease; }
    .screen img.active { opacity:1; }
    .controls { display:flex; gap:8px; padding:12px; margin-top:12px; justify-content:center; }
    .dot { width:12px; height:12px; border-radius:50%; background: rgba(255,255,255,0.4); cursor:pointer; transition: transform .3s; }
    .dot.active { background: linear-gradient(90deg,var(--accent1),var(--accent2)); box-shadow: 0 0 12px rgba(255,182,250,0.8); transform: scale(1.3); }
    .btns { margin-top:18px; display:flex; justify-content:center; gap:14px; }
    button { padding:10px 18px; border:none; border-radius:20px; font-size:16px; cursor:pointer; background: linear-gradient(90deg,var(--accent1),var(--accent2)); color:#fff; font-weight:bold; box-shadow:0 5px 15px rgba(0,0,0,0.2); transition:transform 0.2s; }
    button:hover { transform: scale(1.1); }
    .modal { position:fixed; top:0; left:0; width:100%; height:100%; background: rgba(0,0,0,0.6); display:flex; align-items:center; justify-content:center; opacity:0; pointer-events:none; transition:opacity 0.4s; z-index:10; }
    .modal.active { opacity:1; pointer-events:auto; }
    .envelope-container { position: relative; width:260px; height:180px; background:#ffecf6; border-radius:6px; overflow:hidden; box-shadow:0 12px 25px rgba(0,0,0,0.25); animation: pop 0.5s ease; }
    .flap { position:absolute; top:0; left:0; width:100%; height:100%; background: linear-gradient(135deg,#ffb2e6,#caa9ff); clip-path: polygon(0 0, 100% 0, 50% 50%); transform-origin: top; transition: transform 1s; z-index:2; }
    .letter { position:absolute; top:100%; left:0; width:100%; height:100%; background:#fff; display:flex; align-items:center; justify-content:center; padding:12px; text-align:center; font-size:14px; line-height:1.4; transition: top 1s; border-radius:6px; z-index:1; }
    .envelope-container.open .flap { transform: rotateX(180deg); }
    .envelope-container.open .letter { top:0; }
    @keyframes pop { 0% { transform: scale(0.7); opacity:0; } 100% { transform: scale(1); opacity:1; } }
    #heartBurst, #starBurst { position: absolute; width:100%; height:100%; top:0; left:0; pointer-events: none; overflow:hidden; }
    #heartBurst span, #starBurst span { position:absolute; opacity:0.9; transition: none; animation-timing-function: ease-out; }
    @keyframes burstUp { from { transform: translateY(0) scale(1); opacity:1; } to { transform: translateY(-150px) scale(1.8); opacity:0; } }
    .modal-content { background: linear-gradient(135deg,#fff0f9,#ffe6ff); border-radius:16px; padding:24px; max-width:320px; text-align:center; box-shadow:0 10px 30px rgba(0,0,0,0.3); animation: pop 0.5s ease; }
    .close { margin-top:10px; cursor:pointer; font-size:13px; color:#333; text-align:center; }
  </style>
</head>
<body>
  <div id="floating"></div>
  <div class="wrap">
    <div class="left">
      <h1 class="headline">Happy Boyfriend's Day my kuchu puchu</h1>
      <p class="lead"> LOVE YOU TO THE MOON AND BACK💖</p>
      <div class="btns">
        <button id="penguin">🐧 Penguin</button>
        <button id="dadduuu">💕 Dadduuu</button>
      </div>
    </div>
    <div class="right">
      <div class="device">
        <div class="screen" id="screen">
          <!-- Sunglasses -->
          <img src="data:image/jpeg;base64,{{BASE64_IMAGE_1}}" class="active" alt="slide1">
          <!-- Cups -->
          <img src="data:image/jpeg;base64,{{BASE64_IMAGE_2}}" alt="slide2">
          <!-- Flexing -->
          <img src="data:image/jpeg;base64,{{BASE64_IMAGE_3}}" alt="slide3">
        </div>
        <div class="controls">
          <div class="dot active"></div>
          <div class="dot"></div>
          <div class="dot"></div>
        </div>
      </div>
    </div>
  </div>
  <!-- Penguin modal -->
  <div class="modal" id="modalPenguin">
    <div>
      <div style="position:relative;">
        <div class="envelope-container" id="envelope">
          <div class="flap"></div>
          <div class="letter">
            <p>💌 To My Penguin 💌<br><br>You are the cutest part of my world. Like a little penguin waddling into my heart, you make everything warmer & brighter. 🐧💕</p>
          </div>
        </div>
        <div id="heartBurst"></div>
      </div>
      <div class="close" onclick="closeModal('modalPenguin')">Close ✨</div>
    </div>
  </div>
  <!-- Dadduuu modal -->
  <div class="modal" id="modalDadduuu">
    <div class="modal-content">
      <h2>🎀 For My Dadduuu 🎀</h2>
      <p>You’re my safe place, my silly partner, and the sparkle to my stars ✨💖. Forever & always, my cutest dream. 🌙💕</p>
      <div id="starBurst"></div>
      <div class="close" onclick="closeModal('modalDadduuu')">Close ✨</div>
    </div>
  </div>
  <script>
    const slides = document.querySelectorAll('.screen img');
    const dots = document.querySelectorAll('.dot');
    let index = 0, timer;
    function showSlide(i) {
      slides.forEach((s,j) => {
        s.classList.toggle('active', j === i);
        dots[j].classList.toggle('active', j === i);
      });
      index = i;
    }
    function nextSlide() { index = (index + 1) % slides.length; showSlide(index); }
    function startAutoPlay() { timer = setInterval(nextSlide, 4000); }
    dots.forEach((dot, i) => { dot.addEventListener('click', () => { clearInterval(timer); showSlide(i); startAutoPlay(); }); });
    startAutoPlay();
    const floating = document.getElementById('floating'); const icons = ["💖","🎀","💜","💕","💗"];
    function spawnFloating() { const span = document.createElement('span'); span.innerText = icons[Math.floor(Math.random()*icons.length)]; span.style.left=(Math.random()*100)+"vw"; span.style.fontSize=(14+Math.random()*20)+"px"; span.style.animationDuration=(8+Math.random()*5)+"s"; floating.appendChild(span); setTimeout(()=>span.remove(),12000);}
    setInterval(spawnFloating, 500);
    const penguinBtn=document.getElementById('penguin'); const dadduBtn=document.getElementById('dadduuu'); const modalPenguin=document.getElementById('modalPenguin'); const modalDadduuu=document.getElementById('modalDadduuu'); const envelope=document.getElementById('envelope'); const heartBurst=document.getElementById('heartBurst'); const starBurst=document.getElementById('starBurst');
    penguinBtn.onclick=()=>{modalPenguin.classList.add('active'); envelope.classList.remove('open'); heartBurst.innerHTML=""; setTimeout(()=>{envelope.classList.add('open'); spawnBurst(heartBurst,["💖","💕","💜","💗"],15);},500);};
    dadduBtn.onclick=()=>{modalDadduuu.classList.add('active'); starBurst.innerHTML=""; setTimeout(()=>{spawnBurst(starBurst,["✨","✦","✧","💫","🌟"],20);},300);};
    function closeModal(id){document.getElementById(id).classList.remove('active'); if(id==="modalPenguin"){envelope.classList.remove('open'); heartBurst.innerHTML="";} if(id==="modalDadduuu"){starBurst.innerHTML="";}}
    function spawnBurst(container, iconsArr, count){for(let i=0;i<count;i++){setTimeout(()=>{const span=document.createElement('span'); span.innerText=iconsArr[Math.floor(Math.random()*iconsArr.length)]; span.style.left=(40+Math.random()*20)+"%"; span.style.bottom="20%"; span.style.fontSize=(16+Math.random()*20)+"px"; const duration=2+Math.random()*1.5; span.style.animation=`burstUp ${duration}s ease-out forwards`; container.appendChild(span); setTimeout(()=>span.remove(),duration*1000+200);},i*100);}}
  </script>
</body>
</html>